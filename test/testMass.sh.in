#! /bin/bash
# @configure_input@

# want to see this output
./testMass@EXEEXT@

OS=`uname`
case "$OS" in
CYGWIN*)
  cmd1=`sed 's/e+0/e+/g'  testMass1.dat  | \
     sed 's/e-0/e-/g'  -  | \
	@DIFF_Q@ - @srcdir@/testMass1.dat`
  cmd2=`@DIFF_Q@ testMass2.dat testMass1.dat`
  ;;
*)
  cmd1=`@DIFF_Q@ testMass1.dat @srcdir@/testMass1.dat`
  cmd2=`@DIFF_Q@ testMass2.dat testMass1.dat`
esac

if [ -n "$cmd1" ]
then
  echo $cmd1
  exit 1;
fi

if [ -n "$cmd2" ]
then
  echo $cmd2
  exit 1;
fi

exit 0;
